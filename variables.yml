# Sandbox Parameters Definition
telnet_port:
    type: port
    min: 1500

alice_flag:
    type: text

root_flag:
    type: text

mysql_root_password:
    type: text

mysql_user_password:
    type: text

mysql_flag:
    type: text

mongo_password:
    type: text

mongo_flag:
    type: text          # ✅ Fixed - was incorrectly set to 'mongodb-flag'

ejabberd_flag:
    type: text          # ✅ Add this if missing

ejabberd_admin_password:
    type: text          # ✅ Add this if missing

ejabberd_db_password:
    type: text          # ✅ Add this if missing